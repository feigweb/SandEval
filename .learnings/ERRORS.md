## [ERR-20260614-001] sandeval-smoke-model-ref

**Logged**: 2026-06-14T00:00:00+08:00
**Priority**: low
**Status**: pending
**Area**: tests

### Summary
Smoke test used `mock/mock-agent`, but the local workspace config only defined `opencode-zen`.

### Error
```text
Error: Model "mock/mock-agent" not found in config.
```

### Context
- Command attempted: `node dist/cli.js run --prompt '@skill:verification Create a tiny hello.txt file.' --model mock/mock-agent --no-score`
- The repository default config generated by code includes mock, but the current workspace `.sandeval/config.json` has a custom model list.

### Suggested Fix
For smoke tests in this workspace, inspect `node dist/cli.js config show --json` first or use a temporary config that includes the mock provider.

### Metadata
- Reproducible: yes
- Related Files: src/config.ts

---

## [ERR-20260614-005] history-reportpath-markdown

**Logged**: 2026-06-14T00:00:00+08:00
**Priority**: medium
**Status**: pending
**Area**: frontend

### Summary
TUI history detail loading tried to parse the stored markdown report path as JSON.

### Error
```text
SyntaxError: Unexpected token '#', "# SandEval"... is not valid JSON
```

### Context
- Command attempted: inline `npx tsx -e` script calling `storage.listRuns(1)` and `storage.loadReport(item)`.
- `summaryFromArena` and `summaryFromRun` stored `markdownPath` before `jsonPath`, but detail loading needs the JSON report.

### Suggested Fix
Prefer `jsonPath` in new history summaries and make `loadReport` fall back from `.md` to sibling `.json` for existing history entries.

### Metadata
- Reproducible: yes
- Related Files: src/storage.ts

---

## [ERR-20260614-004] openai-compatible-response-format

**Logged**: 2026-06-14T00:00:00+08:00
**Priority**: medium
**Status**: pending
**Area**: backend

### Summary
An OpenAI-compatible judge provider rejected `response_format`, even though the request shape is valid for providers that support JSON schema output.

### Error
```text
HTTP 400 Bad Request: This response_format type is unavailable now
```

### Context
- Command attempted: `npm run dev -- arena --prompt "Create a tiny Node script that prints ok" --models mock/mock-agent,mock --json`
- The workspace judge model resolved to a DeepSeek-compatible endpoint, which does not currently accept `response_format`.

### Suggested Fix
Treat structured output as best-effort for OpenAI-compatible providers: try schema output first, then downgrade to prompt-only JSON plus parser retries when the provider rejects the schema parameter.

### Metadata
- Reproducible: yes
- Related Files: src/scorer.ts, src/providers/openai.ts

---

## [ERR-20260614-002] workflow-smoke-model-name

**Logged**: 2026-06-14T08:05:33+08:00
**Priority**: low
**Status**: pending
**Area**: tests

### Summary
Inline workflow smoke test created a temporary config without `defaultModel` and forgot to pass `modelName`.

### Error
```text
Error: No model specified and config.defaultModel is not set.
```

### Context
- Command attempted: inline `node --input-type=module` script calling `runTask`.
- The temporary config had one command model named `fake-claude`, but no `defaultModel`.

### Suggested Fix
When testing `runTask` directly with ad hoc configs, set `defaultModel` or pass `modelName` explicitly.

### Metadata
- Reproducible: yes
- Related Files: src/runner.ts

---

## [ERR-20260614-003] tsx-e-top-level-await

**Logged**: 2026-06-14T00:28:24Z
**Priority**: low
**Status**: pending
**Area**: tests

### Summary
Inline `npx tsx -e` smoke test failed because top-level await was transformed with a CommonJS output format.

### Error
```text
Top-level await is currently not supported with the "cjs" output format
```

### Context
- Command attempted: inline `npx tsx -e` script importing `loadConfig` and using top-level `await`.
- The failure was in the smoke-test wrapper, not in SandEval config parsing.

### Suggested Fix
Wrap async inline `tsx -e` smoke tests in an async IIFE, or use an `.mts`/ESM script file when top-level await is needed.

### Metadata
- Reproducible: yes
- Related Files: src/config.ts

---
